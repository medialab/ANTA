<?php	$this->headLink()->appendStylesheet( Anta_Core::getBase() . '/css/today-special.css');		echo 	Ui_Board::getInstance( "TodaySpecial", array( 		'user'		=> $this->user,		'document'	=> $this->document	));?><!-- co-occurrences list and filtering features --><script type='text/javascript'>		$(document).ready( function(){				var form = $("#form-co-occurreces-form");				// apply filter and submit form		$(".lens-filter").click( function(){									var newActionUrl = "<?php $_SERVER['REDIRECT_URL'] ?>" + $(this).attr( 'href' );						// apply new link using filters			form.attr( 'action', newActionUrl );			form.submit();						return false;		});				$(".match-against-item").click( function(){			 event.preventDefault();			// change form query value, then submit			$("#query").val( $( this ).attr( 'title' ) );			var newActionUrl = "<?php $_SERVER['REDIRECT_URL'] ?>" + $(this).attr( 'href' );						// apply new link using filters			form.attr( 'action', newActionUrl );			form.submit();			return false;					});	});</script><?php echo $this->dock ?><!-- -->